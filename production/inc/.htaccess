# Bloque tout accès web direct à ce dossier (configuration, fonctions, logs).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
